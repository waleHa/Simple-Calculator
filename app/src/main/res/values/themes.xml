<resources>
    <!-- Base application theme. -->
    <style name="Theme.Calculator" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand</item>
        <item name="colorPrimaryVariant">@color/brand</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/background</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="calculator_button">
        <item name="android:textSize">@dimen/text_button</item>
    </style>
<style name="calculator_button.number">
    <item name="android:background">@drawable/circle_button_white</item>
    <item name="android:textColor">@color/black</item>
</style>
<style name="calculator_button.operation">
    <item name="android:background">@drawable/circle_button</item>
    <item name="android:textColor">@color/white</item>
</style>

    <style name="calculator_button.result">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">@color/white</item>
    </style>
<!--    <item name="android:background">@drawable/circle_button</item>-->
    <color name="black">#000000</color>
</resources>